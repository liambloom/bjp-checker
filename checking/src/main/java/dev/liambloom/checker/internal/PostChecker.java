package dev.liambloom.checker.internal;

import dev.liambloom.checker.TestStatus;
import dev.liambloom.checker.books.Post;
import dev.liambloom.checker.books.Result;

public final class PostChecker {
    public static Result<TestStatus> check(Post conditions, Object value) {
        throw new IllegalStateException("Post conditions not yet supported");
    }
}
