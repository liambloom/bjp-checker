package dev.liambloom.checker.books;

public class Post {
    // Result<TestStatus> check(Object object); TODO: implement a `Post` checker in the checker module
    private Object exactValue;
    private ClassConditions checker;
}
